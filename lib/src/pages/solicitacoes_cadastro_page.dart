import 'package:flutter/material.dart';
import '../templates/solicitations_template.dart';

class SolicitacoesCadastroPage extends StatelessWidget {
  const SolicitacoesCadastroPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SolicitationsTemplate();
  }
}
